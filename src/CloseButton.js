import styled from 'styled-components'

const CloseButton = styled.button`
  appearance: none;
  border: none;
  outline: none;
  color: white;
  background: none;
  padding: 0 20px;
  cursor: pointer;
`

export default CloseButton
